{
    "name": "codemirror-highlight-groups",
    "version": "0.0.0",
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc && node build.js",
        "prepare": "npm run build"
    },
    "devDependencies": {
        "esbuild": "^0.28.2",
        "typescript": "^7.0.2"
    },
    "peerDependencies": {
        "codemirror": "^6.0.2"
    },
    "dependencies": {
        "nanoid": "^6.0.1"
    }
}
